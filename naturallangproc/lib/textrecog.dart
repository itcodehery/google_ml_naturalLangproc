import 'package:flutter/material.dart';

class Textrecog extends StatelessWidget {
  const Textrecog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
